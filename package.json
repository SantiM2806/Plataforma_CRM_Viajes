{
  "name": "travelkit-crm",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:migrate": "node packages/db/migrate.mjs",
    "db:make-superadmin": "node packages/db/scripts/make-superadmin.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "turbo": "^2.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
